# WS-50-ESTRA semantic review packet

## Review status

`PREPARED__GUARDED_BASELINE_PASS__SEMANTIC_ACCEPTANCE_OPEN`

The candidate freeze manifest binds 40 files found as modified or untracked
in the source checkout before this worker's evidence mutation. They remain
`PRESERVE_INHERITED`; no candidate bytes were copied into this branch.

## Version separation

### ESTRA Projection Framework 2.0.0-rc.1

- The documents define an additive projection-based EA candidate with an
  explicit OC-to-EA derivation boundary, intent/projection admission,
  deterministic registry semantics and negative scenarios.
- The release receipt records 9 candidate conformance tests and a historical
  52-test repository regression at its 2026-08-08 cut. Its ceiling is
  syntactic and deterministic semantic conformance, not live enterprise
  validation or external certification.
- The machine review records 13 deterministic package checks, including
  lineage non-promotion and content-addressed manifest replay. It is not an
  external human peer review.

### ESTRA Optimality Framework 3.0.0-alpha.1

- The documents define an additive characterization successor over a declared
  experimental interface, predictive-state equivalence and Blackwell/channel
  comparison. They explicitly preserve 2.0 as a distinct predecessor.
- The alpha receipt records 26 scoped v2/v3 tests and an internal finite
  deterministic characterization. Its certificate ceiling is internal
  research alpha; it excludes external peer review, standards certification,
  live enterprise deployment and universal EA completeness.
- Phase C preserves the first admitted Logion typed-history candidate as
  rejected for lack of holdout dominance. That negative result is a constraint,
  not a positive superiority result.

## Cross-surface risks requiring independent review

1. **Evidence freshness:** the inherited release receipts are historical
   candidate evidence and must not be reused as a fresh 69-test baseline.
2. **Dominance boundary:** no typed-history dominance may be claimed while the
   holdout delta is negative and CI includes zero.
3. **Lineage separation:** 2.0 projection conformance and 3.0 optimality
   characterization must remain separate package claims and commit surfaces.
4. **Scope/conformance:** finite deterministic executable checks do not support
   the broader measurable-channel, stochastic or live-enterprise claims.
5. **Transport readiness:** dirty/untracked inherited files need owner/worker
   classification, exact diff review, and a fresh guarded test receipt before
   any canonical transport decision.

## Review decision

`HOLD_SEMANTIC_ACCEPTANCE`

The material is coherent enough for bounded internal candidate review, but this
initial worker checkpoint does not accept either candidate for Toolkit or
publication transport. The fresh guarded 69-test reproduction passed after the
repository's declared `src` import root was supplied. The earlier safety
refusal and unqualified collection failures remain recorded diagnostics.
Independent semantic review and transport acceptance remain open.
