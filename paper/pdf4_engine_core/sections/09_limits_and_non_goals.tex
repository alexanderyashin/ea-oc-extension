\section{Limits and Non-Goals}

This section defines the \textbf{explicit limits} of the Engine Core and clarifies
what is intentionally excluded from PDF-4. These limits are constitutive: they
define the boundary of admissible interpretation for OPS-1 diagnostic semantics.

\subsection{Interpretation boundaries}

Diagnostic outputs produced by the Engine Core are \emph{structural
classifications only}. They express admissibility or non-admissibility with
respect to declared invariants and terminal STOP conditions.

Any interpretation beyond invariant satisfaction is external to the kernel and
has no standing within OPS-1.

\paragraph{No semantic lifting.}
Consumers of diagnostic output must not treat classifications or STOP as:
\begin{itemize}
  \item recommendations, guidance, or action directives;
  \item root-cause analyses or explanatory narratives;
  \item forecasts, predictions, or risk estimates;
  \item compliance judgments, managerial assessments, or performance evaluations.
\end{itemize}

Such interpretations require additional assumptions, evidence, or authority that
are explicitly excluded from the Engine Core boundary.

\subsection{OPS-1 vs.\ OPS-2 responsibility split}

OPS-1 is a \textbf{contract and authority layer}. It fixes admissibility,
invariants, terminal STOP semantics, determinism, reproducibility, and provenance
identity.
OPS-1 does not address usage, presentation, tooling, orchestration, or
decision-making.

Any toolkit surface (CLI/GUI/scripts/integrations) belongs to OPS-2 and is an
\textbf{exchangeable consumer} of OPS-1. OPS-2 may display, transport, or package
results, but it must not define, reinterpret, operationalize, or extend OPS-1
semantics.

\paragraph{Binary compliance.}
Compliance is \textbf{binary}: conformant or \emph{non-ESTRA}. Intent, usability
goals, product considerations, or domain expectations do not soften this
boundary.

\paragraph{Auto-disqualification by category error.}
Any representation of OPS-1 as a ``method'', ``tool'', ``framework'',
``best practice'', ``playbook'', or ``how-to'' is a category error.
If an OPS-2 layer (or its documentation) implies ``you should do X'' from
classification or terminal STOP, it introduces prescriptive semantics and is
therefore \textbf{non-ESTRA}.

\subsection{Known non-diagnostic cases}

The Engine Core cannot produce admissible diagnostic results when:
\begin{itemize}
  \item required observability is missing, indeterminate, or explicitly absent;
  \item invariants are inconsistent, underspecified, or contradictory;
  \item threshold objects are ambiguous, incompatible, or ill-defined;
  \item kernel invariants are violated;
  \item provenance completeness cannot be established;
  \item determinism or reproducibility, as architectural invariants, is violated.
\end{itemize}

In all such cases, diagnostic admissibility itself is violated and terminal STOP
applies. STOP does not compensate for these conditions; it denotes their formal
recognition.

\subsection{What requires external evidence or authority}

The following concerns necessarily require evidence, processes, or authority
\emph{outside} the Engine Core:
\begin{itemize}
  \item remediation planning or corrective action design;
  \item prioritization, sequencing, or trade-off resolution;
  \item assignment of organizational responsibility or accountability;
  \item economic, performance, or impact evaluation;
  \item governance, approval, or decision authority.
\end{itemize}

PDF-4 explicitly declines to address these concerns in order to preserve the
diagnostic neutrality and ontological integrity of the kernel.

\subsection{Explicit exclusion of workflows and methods}

PDF-4 does not define workflows, procedures, methods, playbooks, recipes, or
execution instructions of any kind.

The diagnostic operation graph defined in PDF-4 exists solely to fix admissibility
boundaries and terminal STOP enforcement. It must not be interpreted as:
\begin{itemize}
  \item a process model or workflow description;
  \item a decision tree or control structure;
  \item an execution sequence to be followed by agents or systems;
  \item a method for optimization, steering, intervention, or management.
\end{itemize}

Any interpretation of the operation graph as a workflow, method, or procedural
guidance is non-admissible under OPS-1 and outside the declared scope of PDF-4.

\subsection{Determinism and reproducibility are not negotiated by process}

OPS-1 fixes determinism and reproducibility as \textbf{architectural invariants}.
Accordingly, PDF-4 does not define, recommend, imply, or tolerate any process-level
mechanisms intended to ``achieve'', ``improve'', or ``enforce'' them.

If determinism or reproducibility would require procedural controls to hold, the
artefact is non-admissible under OPS-1 and produced outputs are invalid.

\subsection{Forbidden extensions (auto-disqualifying)}

The following extension classes are \textbf{explicitly forbidden}.
\textbf{If any of them is present, the artefact is NOT ESTRA-conformant.}

\paragraph{Semantic control and steering.}
\begin{itemize}
  \item any optimization, recommendation, prioritization, or decision semantics;
  \item any control logic influencing or steering diagnostic outcomes;
  \item any feedback edge or adaptive branching affecting evaluation.
\end{itemize}

\paragraph{Learning and adaptation.}
\begin{itemize}
  \item any form of learning, fitting, calibration, or adaptation influencing
        invariants, thresholds, classification, or terminal STOP;
  \item any use of historical cases to modify kernel behavior.
\end{itemize}

\paragraph{STOP violation.}
\begin{itemize}
  \item any retry, recovery, bypass, or continuation path after terminal STOP;
  \item any reinterpretation of STOP as warning, error, signal, or advisory.
\end{itemize}

\paragraph{Non-determinism and provenance violation.}
\begin{itemize}
  \item any semantically relevant non-determinism;
  \item any partial, probabilistic, inferred, or reconstructed provenance;
  \item any post-hoc correction, repair, normalization, or retrofitting of evidence.
\end{itemize}

\paragraph{Registry and evidence manipulation.}
\begin{itemize}
  \item mutable registries or evidence stores;
  \item overwriting, replacing, deleting, or rewriting evidence artefacts;
  \item opaque preprocessing or hidden sampling or filtering.
\end{itemize}

\paragraph{UX-driven semantics.}
\begin{itemize}
  \item any user interface, visualization, or interaction layer that alters,
        constrains, or implies diagnostic meaning;
  \item any UX-driven simplification that hides terminal STOP conditions or
        admissibility boundaries.
\end{itemize}

Presence of any forbidden class immediately disqualifies the artefact from OPS-1
and ESTRA conformance, regardless of tooling, intent, or external justification.
