\section*{Abstract (Skeleton)}
This document defines the \emph{OPS-0 artifact class}: an \textbf{Operational Semantics \& Tooling Contract}.
It is a scope-locked, diagnostic-only contract scaffold that specifies what \emph{may} be defined in later runs
and what is explicitly out of scope here.

\textbf{Status:} placeholders only. No operational semantics, no registries, no examples, no implementations.

\textbf{Reserved for: RUN-OPS0-02+ (content population).}