\section{Purpose and Scope (Locked)}
\subsection{Purpose}
OPS-0 is a \textbf{contract scaffold} that will later constrain tooling behavior and admissible transformations
around EA-OC-EXTENSION artifacts, without adding new theoretical primitives.

\subsection{Scope Lock}
This RUN-OPS0-01 deliverable provides only:
\begin{itemize}
  \item the locked artifact class statement (\emph{Operational Semantics \& Tooling Contract});
  \item strict non-claims and diagnostic-only boundary conditions;
  \item section map and placeholders for future contract tables (appendix registries);
  \item lineage statement to PDF-0 / PDF-2 / PDF-3 (separation of concerns).
\end{itemize}

Everything else is explicitly deferred.

\subsection{Reserved}
All substantive content is reserved for RUN-OPS0-02 and subsequent runs.