\section{Operational Primitives (Headings Only)}
\subsection{Non-Introduction Rule}
RUN-OPS0-01 introduces \textbf{no new OC primitives} and performs \textbf{no reinterpretation} of ETS.K\_EA.v1.1 semantics.

\subsection{Placeholders (to be specified)}
The following headings exist as placeholders only:
\begin{itemize}
  \item Artifact identifiers and version labels (format constraints to be specified).
  \item Tool actions / operations (registry to be specified).
  \item STOP / NO-GO enforcement surface (mechanism to be specified).
  \item Provenance and signature surface (mechanism to be specified).
\end{itemize}

\textbf{Reserved for: RUN-OPS0-02.}